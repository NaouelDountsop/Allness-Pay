/**
 * Point d'entree de l'arbre applicatif.
 *
 * C'est ici que viendra le routeur (`react-router-dom`), une fois les premieres
 * fonctionnalites creees sous `src/features/`. Voir `src/features/README.md`.
 */
export function App() {
  return <div id="app-shell" />;
}
